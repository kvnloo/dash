# Deep pass — 2026-09-12

Status: **working capture**. Ten full-paper agents in flight (`composer-2.5`, not min-effort). This file is what we can already **prove on fixtures** without waiting for them. Schema untouched. Not a paper.

## 1. Edges cannot carry a session type

`schema/hotl-0.2.schema.json`: `$defs.edge.additionalProperties` is **false**. Root is also closed. `policies` and `constraints.budgets` are open objects.

Optional MPST/Pact global type therefore lives under **`policies`** (compiler profile), never as a new edge field. That is the rMPST hole closed without HOTL 0.3.

## 2. Static \(\pi_v\) on every 0.2 valid fixture

`docs/research/tools/pi_v.py` against `/tmp/research/aodl/examples/valid`:

| Fixture | nodes | issues | note |
|---|---|---|---|
| craid | 10 | 0 | every \|P(v)\|=2; analysis fan-in declared reducer |
| pipeline | 4 | 0 | |
| fanout-fanin | 4 | 0 | |
| market | 7 | 0 | |
| bounded-recursion | 3 | 0 | child wired=2 |

This is only the **port-wiring** leg. MPST locals and \(\lambda_A\) arity still need a compiler witness (paper gate 1).

## 3. Hermes dry-run is fail-closed interchange

`docs/research/tools/hermes_dryrun.py` — Kanban cards+deps only. No spawn, no API.

| Doc | exit | why |
|---|---|---|
| `pipeline.json` | **0** | kinds `sequence, retry`; dependency edges only → 4 cards, 2 deps |
| `craid.json` | **1** | `fanout` + `reducer` — C(RAID) is not the gate-1 fixture |
| `market.json` | **1** | `auction` + `allocation` edges |

That is the working-note sentence as a CLI: conservative projection, not a second scheduler. Full C(RAID) needs a **richer profile** (fanout/reducer/human_gate), still not a runtime.

## 4. Catalog kit (PER-1460)

TDD order in the aodl clone: `e2e/catalog.spec.js` `SECTIONS` gained **Readings** (after three objects) and **Adapters** (after C(RAID)), then `Language.jsx` bound the draft copy. Exact files in [`kit/`](kit/). Live Pages still require a token that can push `kvnloo/aodl`.

No `topos` in the UI. No inferred swarm. No schema fields.

## 5. Deep-read wave — closed (10/10)

Notes in [`deep/`](deep/). Full papers (ar5iv / arXiv HTML / PDF / LaTeX). Jina was blocked or stubbed on several (Pact, AdaptOrch, RLM). Schema untouched. Do not remint \(\mathcal{O}_t\).

| Note | Source | Keep | Do not |
|---|---|---|---|
| [`lambda-a.md`](deep/lambda-a.md) | [2604.11767](https://arxiv.org/abs/2604.11767) | Intra-node calculus; 786/835 YAML **ERROR** is lint, not “runtime broken”; 46 clean | Replace HOTL with a lambda |
| [`agentflow.md`](deep/agentflow.md) | [2607.01640](https://arxiv.org/abs/2607.01640) | Recovered ADG is **audit**; fail closed if ADG ⊭ intent | Treat recovered graph as observed \(\mathcal{O}_t\); map onto `agentenv/agentflow` |
| [`pact.md`](deep/pact.md) | [2605.03143](https://arxiv.org/abs/2605.03143) | Global type → local projection on `message`/`delegation`; game = \(\Pi_t\) | Payment / `exchange`; Nash solver as runtime |
| [`stp-sheaves.md`](deep/stp-sheaves.md) | [2605.01879](https://arxiv.org/abs/2605.01879) | \(F_{World}/F_{Mem}/F_{Goal}\) stay named; obstruction = fail closed; mutation = natural transformation on World | JSON a topos; rename World→observed |
| [`rlm-slice.md`](deep/rlm-slice.md) | [2512.24601](https://arxiv.org/abs/2512.24601) v3 | Prompt-as-env (Alg 1); slice vs dump; **stratify** 8 O(1)+8 linear+8 pairwise | Cite RLM as interchange proof |
| [`merge-neq-verify.md`](deep/merge-neq-verify.md) | [2607.05844](https://arxiv.org/abs/2607.05844) × [2510.18893](https://arxiv.org/abs/2510.18893) | OpSet ∪ then ConflictSet; projection may **abstain**; 5–10% semantic survivors | New kinds; CRDT as verifier |
| [`rmpst-refinements.md`](deep/rmpst-refinements.md) | [2501.18874](https://arxiv.org/abs/2501.18874) | Legal frame ≠ legal trace; optional `policies.choreo` | Session types on closed edges |
| [`adaptorch.md`](deep/adaptorch.md) | [2602.16873](https://arxiv.org/abs/2602.16873) | \(\tau_P/\tau_S/\tau_H/\tau_X\) → fanout / sequence / delegation / **declared** hybrid; topology > model after ε-convergence | Swarm from TikZ; unlabeled Crew hybrid |
| [`hermes-craid-projection.md`](deep/hermes-craid-projection.md) | local fixtures | Gate 1 **MAY** project `e-product-research` only | Full `craid.json` as the gate-1 fixture |
| [`craid-attention.md`](deep/craid-attention.md) | fixture × [2606.08919](https://arxiv.org/abs/2606.08919) | Captain `control`+`observation` is a flooding surface; `budgets.humanAttention` is schema-legal | Second human-gate kind |

### Cross-cuts (do not reopen)

1. **Three objects stay three.** AgentFlow recovers code; RLM slices a string; STP sheaves are not intent/plan/observed under other names.
2. **\(\Pi_t\) is selection, not a SKU.** Pact’s game, AdaptOrch’s router, auction allocation — declared policy. Search over validated programs after ε-convergence (Evo-Bench \(H_t\)), fail-closed on unlabeled hybrid.
3. **Choreo lives on `policies`, never on edges.** rMPST refinements and Pact projection are compiler profiles. MCP/A2A can be schema-valid **and** out-of-order-unsafe.
4. **Merge ≠ verify.** StateFuse ConflictSet + CodeCRDT residual semantics. Verifier paths share the captain’s `humanAttention` ledger.
5. **Paper gates still open.** Hermes dry-run CLI exists (pipeline exit 0, craid/market exit 1). Keel L0 and the **measured** slice-vs-dump experiment are unrun. RLM supplies the strata, not the proof.

Next R (not this commit): CoMPSeT, MASFactory, AgentSpawn, CodeCRDT full text, Evo-Bench, HOTL-legal, MCP census, LiveMem, AgentRoom, Qoreo — still readings/adapters, still no HOTL 0.3.
