# AODL catalog kit (PER-1460)

Paste into `kvnloo/aodl` (this cloud token cannot push that repo).

TDD: `language/e2e/catalog.spec.js` `SECTIONS` first (must fail), then `language/src/Language.jsx`. Existing `language.css` tables — no new `details`, no `design.html`, no inferred swarm.

Proof of fail-then-pass belongs on the aodl tree. Dash only holds the files.

| Copy into aodl | From |
|---|---|
| `language/src/Language.jsx` | [`Language.jsx`](Language.jsx) |
| `language/e2e/catalog.spec.js` | [`catalog.spec.js`](catalog.spec.js) |
